class Constants:
    """
    Constant class that contains static variables
    """

    WINDOW_WIDTH = 800
    WINDOW_HEIGHT = 800
    GRID_SIZE = 40
    GRID_ELEMENT_WIDTH = float(WINDOW_WIDTH / GRID_SIZE)
    GRID_ELEMENT_HEIGHT = float(WINDOW_HEIGHT / GRID_SIZE)
    WALLS_TO_REMOVE = 50


